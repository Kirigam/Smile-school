 $(document).ready(function ($) {
   $('.galaryCon').smile({
   		button: false,
   		background: "000000",
   		alpha: 0.6,
   		autoplay: {
   			bool: true,
   			interval: 1000
   		},
    });
});